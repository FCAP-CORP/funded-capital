import PricingClient from "./PricingClient";

export const metadata = {
  title: "Price a Deal | Funded Capital Broker Portal",
};

export default function PricePage() {
  return <PricingClient />;
}
